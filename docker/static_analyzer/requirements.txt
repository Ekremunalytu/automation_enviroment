# Static analyzer runtime deps (ES-2, ADR 0016). Minimal by design: the
# hardened container imports only the pydantic-based static-detection
# contracts. pyyaml is pre-staged for the ES-4 Semgrep rule loader (zero
# runtime cost now, avoids an image change when ES-4 enrolls). NO semgrep yet.
pydantic>=2.0.0,<3.0.0
pyyaml>=6.0,<7.0
